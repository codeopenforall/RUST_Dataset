        let safe_input = {
            locked.clone()
        };
        let output = Command::new("echo")
                        .arg(safe_input)
    let input = if args.len() > 1 {
    let executor = Executor::new(input);
