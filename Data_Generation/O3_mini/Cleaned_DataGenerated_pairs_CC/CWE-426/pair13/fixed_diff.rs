use std::path::Path;
    if Path::new(cmd_str).is_absolute() {
        cmd: "/usr/bin/safe_app".to_string(), 
            let code = loader_clone.run();
            if code != 0 {
                std::process::exit(1);
            }
