struct Worker;
impl Worker {
    pub fn execute(&self, data: Vec<u32>) -> u32 {
            data.iter().sum()
    let worker = Worker;
    worker.execute(data)
    let worker = Worker;
    let res = worker.execute(input);
